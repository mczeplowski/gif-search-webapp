import React from 'react';
import { Header, Logo } from './HeaderStyles';

const HeaderComponent = () => (
  <Header>
    <Logo>
      <span>Gif</span>
      <span>Search</span>
    </Logo>
  </Header>
);

export default HeaderComponent;
